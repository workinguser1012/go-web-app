replicaCount: 1

image:
  repository: workinguser1210/go-web-app
  pullPolicy: IfNotPresent
  tag: "26339071812"

ingress:
  enabled: true
  className: "nginx"
  annotations: {}
  hosts:
    - host: go-web-app.local
      paths:
        - path: /
          pathType: Prefix